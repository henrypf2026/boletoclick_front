NEXT_PUBLIC_API_URL=http://localhost:3000

# URL pública para QR (usá la IP de red en dev para escanear desde el celular)
# NEXT_PUBLIC_APP_URL=http://192.168.1.246:3002

NEXT_PUBLIC_SUPABASE_URL=          # URL del proyecto en Supabase (Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=     # Anon/public key del proyecto en Supabase (Settings > API)
NEXT_PUBLIC_MAPBOX_TOKEN=          # Token público de Mapbox (Account > Access tokens)
